import 'package:flutter/material.dart';

class CleanPlayersScreen extends StatelessWidget {
  const CleanPlayersScreen({Key? key}) : super(key: key);
  static const routeName = '/cleanup';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
